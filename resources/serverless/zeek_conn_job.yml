# DSL Lite — Zeek Conn SSS Multi-Task Job (Serverless)

resources:
  jobs:
    zeek_conn_pipeline:
      name: "[${bundle.target}] DSL Lite - Zeek Conn Pipeline"

      environments:
        - environment_key: default
          spec:
            client: "1"

      tasks:

        - task_key: bronze
          description: "Ingest Zeek Conn logs into the Bronze Delta table via Auto Loader."
          environment_key: default
          notebook_task:
            notebook_path: ../../src/sss_bronze.py
            base_parameters:
              preset_file:           "${workspace.file_path}/pipelines/zeek/conn/preset.yaml"
              bronze_database:       "${var.bronze_database}"
              checkpoints_location:  "${var.sss_checkpoints_base}/zeek_conn"
              continuous:            "False"

        - task_key: silver
          description: "Parse and structure Bronze data into the Silver Delta table."
          depends_on:
            - task_key: bronze
          environment_key: default
          notebook_task:
            notebook_path: ../../src/sss_silver.py
            base_parameters:
              preset_file:           "${workspace.file_path}/pipelines/zeek/conn/preset.yaml"
              bronze_database:       "${var.bronze_database}"
              silver_database:       "${var.silver_database}"
              checkpoints_location:  "${var.sss_checkpoints_base}/zeek_conn"
              continuous:            "False"

        - task_key: gold
          description: "Map Silver data to OCSF-compliant Gold tables (network_activity)."
          depends_on:
            - task_key: silver
          environment_key: default
          notebook_task:
            notebook_path: ../../src/sss_gold.py
            base_parameters:
              preset_file:           "${workspace.file_path}/pipelines/zeek/conn/preset.yaml"
              silver_database:       "${var.silver_database}"
              gold_database:         "${var.gold_database}"
              checkpoints_location:  "${var.sss_checkpoints_base}/zeek_conn"
              continuous:            "False"

      permissions:
        - level: CAN_VIEW
          group_name: users
